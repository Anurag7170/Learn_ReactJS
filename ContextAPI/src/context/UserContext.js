import React, { createContext } from 'react'

// craeted context using createContext function
//theis will store the data which will be share to the application
const UserContext = createContext();

export default UserContext